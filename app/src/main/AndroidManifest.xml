<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.smilehacker.megatronsample"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.smilehacker.megatronsample.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".act.Act1"
            android:launchMode="singleTask"
            > </activity>
        <activity android:name=".act.Act2"> </activity>
        <activity android:name=".act.Act3"> </activity>
    </application>

</manifest>